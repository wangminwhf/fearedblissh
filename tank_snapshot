#!/bin/bash

SPATH="/usr/local/sbin"

# Takes a controlled snapshot of each dataset in the 'tank' pool
${SPATH}/zfs_snapshot tank/gentoo/root
${SPATH}/zfs_snapshot tank/gentoo/home
${SPATH}/zfs_snapshot tank/gentoo/src
${SPATH}/zfs_snapshot tank/gentoo/portage
